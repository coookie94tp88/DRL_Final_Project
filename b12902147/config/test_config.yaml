environment:
  num_players: 10
  num_doors: 4
  num_rounds_per_episode: 10
  initial_balance: 1000
  theta: 0.25

training:
  learning_rate: 0.0003
  batch_size: 64
  total_timesteps: 20

checkpointing:
  save_interval: 1
  checkpoint_dir: "./checkpoints/"
  log_dir: "./logs/"
